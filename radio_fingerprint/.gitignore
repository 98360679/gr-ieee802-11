# Large / reproducible artifacts — regenerated by build_dataset.py & train.py
artifacts/

# Datasets & cached windows (build_dataset.py → windows_L*.npz)
*.npz

# Model checkpoints / weights (train.py → *_best.pt)
*.pt
*.pth
*.ckpt

# Metrics / run summaries
*_metrics.json
summary.json

# Python caches
__pycache__/
*.pyc

# NOTE: figs/ is intentionally tracked — the EDA plots, confusion matrices and
# comparison charts are committed results. Don't ignore them.
